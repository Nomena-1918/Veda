// import veda.User;
// import veda.reflect.Reflect;

public class Run{
    public static void main(String[] args) throws Exception{
        // JDBC<User> jdbc=new JDBC<>();
        // jdbc.setTable("users");
        // User user=new User();
        // user.setNom("Risa");
        // user.setAge(48);
        // user.setDateNaiss(new DateGen(1996, 7, 2));
        // jdbc.setObject(user);
        // // System.out.println(jdbc.getUpdateQuery());
        // Vector<User> list=jdbc.selectAll(null);
        // for(User us:list){
        //     System.out.println(us.getNom());
        //     System.out.println(us.getAge());
        //     System.out.println(us.getDateNaiss());
        // }
        // jdbc.inserer(null);
        // jdbc.update(null, new String[]{"nom", "dateNaiss"});
        // jdbc.deleteAll(null);
        // Connect connect=new Connect("localhost", "data1", "eriq66", "root");
        // Connection connex=connect.getPostgre();
        // PreparedStatement statement=connex.prepareStatement(jdbc.getInsertQuery());
        // statement.setString(1, "Risa");
        // statement.setInt(2, 19);
        // statement.setDate(3, user.getDateNaiss().toDateOnly().toDateSql());
        // statement.executeUpdate();
        // connex.commit();
        // statement.close();
        // connex.close();
        // Reflect reflect=new Reflect(User.class);
        // System.out.println(reflect.getValueOfAnnot("IsRelation", "table"));
    }
}